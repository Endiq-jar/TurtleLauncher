<?xml version="1.0" encoding="utf-8"?>
<!-- TurtleLauncher: dialogs now rise slightly as they scale in, and land with a small
     overshoot, so opening one feels like it popped rather than just appeared. -->
<set xmlns:android="http://schemas.android.com/apk/res/android">

    <scale
        android:interpolator="@android:interpolator/overshoot"
        android:fromXScale="0.88"
        android:fromYScale="0.88"
        android:toXScale="1.0"
        android:toYScale="1.0"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="280" />

    <translate
        android:interpolator="@android:interpolator/fast_out_slow_in"
        android:fromYDelta="4%p"
        android:toYDelta="0"
        android:duration="240" />

    <alpha
        android:interpolator="@android:interpolator/fast_out_slow_in"
        android:fromAlpha="0.0"
        android:toAlpha="1.0"
        android:duration="160" />
</set>
