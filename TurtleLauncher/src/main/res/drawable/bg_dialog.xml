<?xml version="1.0" encoding="utf-8"?>
<!-- TurtleLauncher dialog surface: solid translucent fill + glowing accent border
     (previously had a gradient glossy-highlight overlay layer - removed per user
     request for solid colors throughout instead of gradients). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base translucent fill -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="@color/turtle_surface" />
        </shape>
    </item>
    <!-- Outer glowing accent stroke -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <stroke android:width="1dp" android:color="@color/turtle_stroke" />
        </shape>
    </item>
</layer-list>
